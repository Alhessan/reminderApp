export const environment = {
  production: true,
  apiUrl: 'https://api.yourdomain.com/api', // Update this with your production API URL
  enableLogging: false, // Disable detailed logging in production
  enableConsoleLogs: false // Disable console logs in production for performance
};
